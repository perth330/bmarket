%body
  .header
    = link_to '#' do 
      = image_tag src="logo.png", width:"200px",height:"50px", class:"header__icon"
      -# とりあえずimage_tag使ってる
  .layout
    .layout__left
    .layout__center 
      %form(action="")
        .login__no__account
          %p アカウントをお持ちでない方はこちら
          = link_to "#", class: "sign__up__btn" do
            新規登録
            -# link先はまだ
        .login__frame
          .form
            %input.form__defalt{type: "email",:placeholder => "メールアドレス"}
          .form
            %input.form__defalt{type: "password",:placeholder => "パスワード"}
        .login__frame
          = link_to "#", class: "login__frame__btn" do
            ログイン
    .layout__right
  .footer
    = link_to '#' do 
      = image_tag src="logo-white.png", width:"200px",height:"50px", class:"footer__icon"
