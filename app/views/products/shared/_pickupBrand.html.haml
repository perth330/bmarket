%section.pickupBrand
  %h1.pickupBrand__title
    ピックアップブランド
  %h2.pickupBrand__post
    = link_to "アーカイバ","#",class: "listBtn-green"
  .brandLists
    .brandLists__list
      - @categoryBrand.each do |product|
        = link_to product_path(product.id), class: "listBtn" do
          %figure.listImages
            -# = image_tag product.images[0].image_url.url ,alt: "image_url",class: "newImage"
          .productBody
            %h3.productBody__name
              = product.name
            %ul.productBody__menu
              %li
                = "#{product.price}円"
              %li
                %i.fa.fa-star.starIcon
                  0
            %p.productBody__taxIn（税込）